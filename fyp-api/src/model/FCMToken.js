const { query } = require('../db');
const  FCMToken = {}

const db_users =  'locobike.bc_1100_users ' // get the id & token only 
const db_category_group = 'category_group'
const district = 'district'
const db_tags = 'tags'


FCMToken.findUsersByDistrictId = () =>{



    //const stmt = `Select token from ${db_users} where id = ?`

}

FCMToken.findUsersByGroupId = () =>{

}